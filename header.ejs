<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> - <%= siteTitle %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="site-header site-header--cultural" data-site-header>
    <div class="container site-header__inner">
      <div class="site-branding" aria-label="网站标题">
        <p class="site-branding__eyebrow">中华时间文明数字叙事站</p>
        <h1 class="site-branding__title">
          <a href="/"><%= siteTitle %></a>
        </h1>
        <p class="site-branding__subtitle">中国古代计时工具与时空观念沉浸式图文体验</p>
        <p class="site-branding__subtitle-en">
          An Immersive Digital Archive of Ancient Chinese Timekeeping Instruments
        </p>
      </div>

      <div class="site-header__actions">
        <form class="site-search" action="/search" method="get" role="search">
          <label class="site-search__label" for="global-search">站内搜索</label>
          <input
            id="global-search"
            class="site-search__input"
            type="text"
            name="q"
            value="<%= typeof keyword !== 'undefined' ? keyword : '' %>"
            placeholder="搜索器物、朝代、视频、文章"
          >
          <button class="site-search__button" type="submit">检索</button>
        </form>
        <button class="theme-toggle" type="button" aria-label="切换日间/夜间模式">
          <span class="theme-toggle__icon" aria-hidden="true"></span>
        </button>
      </div>
    </div>

    <div class="site-nav-wrap">
      <div class="container">
        <%
          const defaultNavItems = [
            { label: '首页', href: '/' },
            { label: '时之脉', href: '/timeline' },
            { label: '器之格', href: '/qizhige' }
          ];

          const headerNavItems = Array.isArray(navItems) && navItems.length
            ? navItems.filter((item) => item.href !== '/search')
            : defaultNavItems;
        %>

        <nav class="site-nav" aria-label="主导航">
          <% headerNavItems.forEach((item) => { %>
            <% const isActive = currentPath === item.href; %>
            <% const extraClass = item.href === '/qizhige' ? 'site-nav__link--qizhige' : ''; %>
            <a
              href="<%= item.href %>"
              class="site-nav__link <%= extraClass %> <%= isActive ? 'is-active' : '' %>"
              <%= isActive ? 'aria-current="page"' : '' %>
            >
              <%= item.label %>
            </a>
          <% }) %>
        </nav>
      </div>
    </div>
  </header>

  <main class="site-main">
    <div class="container">
